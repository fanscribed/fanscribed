# Use fanscribed itself out of the working copy.
# (Because of the way pip works,
# make sure you're in the top level of your working copy.)
-e .

# Third-party dependencies
Django==1.5.1
django-braces==1.0.0
django-model-utils==1.3.1
ipython==0.13.2
logutils==0.3.3
South==0.7.6
